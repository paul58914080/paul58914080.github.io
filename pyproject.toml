[project]
name = "paul58914080.github.io"
version = "1.0.0"
description = "My personal website built with MkDocs and Material for MkDocs."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mkdocs",
    "mkdocs-material>=1.6.1",
    "pymdown-extensions",
]
